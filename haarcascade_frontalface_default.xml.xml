import java.net.URL;

        public class Utils {
        public static String getCascadePath(String fileName) {
        // Load file from resources folder
        URL resource = Utils.class.getClassLoader().getResource(fileName);
        if (resource == null) {
        throw new RuntimeException("Cascade file not found: " + fileName);
        }
        return resource.getPath();
        }
        }
